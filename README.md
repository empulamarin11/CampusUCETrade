# CampusUCETrade (Monorepo)

Monorepo managed with Turborepo.
Branches: DEV, QA, PROD (main).
## PaaS Usage
The Web Frontend is deployed on a PaaS platform (Vercel/Netlify). This fulfills the “AWS + PaaS” requirement by hosting the user interface as a managed service.
