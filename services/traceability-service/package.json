{
  "name": "@campus/traceability-service",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "lint": "python -m ruff check .",
    "test": "python -m pytest -q || exit 0",
    "build": "echo \"traceability-service: build ok\"",
    "format": "python -m ruff format ."
  }
}
