{
  "name": "@campus/user-service",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build": "echo \"user-service: build\"",
    "lint": "python -m ruff check .",
    "test": "python -m pytest -q || exit 0",
    "format": "echo \"user-service: format\""
  }
}
